@model IEnumerable<NativaGaragem.Models.Solicitacao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Limpeza.Tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Carro.Modelo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Limpeza.Tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Carro.Modelo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDSolicitacao }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDSolicitacao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDSolicitacao })
        </td>
    </tr>
}

</table>
