@model IEnumerable<NativaGaragem.Models.Vaga>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Carro.Modelo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Garagem.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Carro.Modelo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Garagem.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDVaga }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDVaga }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDVaga })
        </td>
    </tr>
}

</table>
